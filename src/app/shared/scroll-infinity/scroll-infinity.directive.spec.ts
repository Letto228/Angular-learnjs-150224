import {ScrollInfinityDirective} from './scroll-infinity.directive';

describe('ScrollInfinityDirective', () => {
    it('should create an instance', () => {
        const directive = new ScrollInfinityDirective();

        expect(directive).toBeTruthy();
    });
});
